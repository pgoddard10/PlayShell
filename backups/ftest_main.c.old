#include "flite_funcs.c"

int main(int argc, char **argv){
	
    if (argc != 2)
    {
        fprintf(stderr,"usage: flite_test FILE\n");
        exit(-1);
    }
    
    generate_tts(argc,argv);
    return 0;
}
